@model IEnumerable<SistemaTurnos.Models.CalidadJuridica>

@{
    ViewBag.Title = "Index";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VTipoCalidadJuridica)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VTipoCalidadJuridica)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdCalidadJuridica }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCalidadJuridica }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCalidadJuridica })
        </td>
    </tr>
}

</table>
