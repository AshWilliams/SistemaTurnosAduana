@model SistemaTurnos.Models.Estado

@{
    ViewBag.Title = "Details";
}

<h2>Detalles</h2>

<div>
    <h4>Estado</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.VDescripcionEstado)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.VDescripcionEstado)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.IdEstado }) |
    @Html.ActionLink("Volver a la Grilla", "Index")
</p>
